import styled from 'styled-components';

export const StyledMediaWrapper = styled.span`
  display: block;
  width: 90%;
  max-width: 860px;
  margin: 4em auto;
  border-radius: 10px;
  aspect-ratio: 16 / 9;
  overflow: hidden;
`